name: Build Release Containers
on:
  push:
    branches: [dev, release]
  release:

jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - run: |
          echo $GITHUB_REF
